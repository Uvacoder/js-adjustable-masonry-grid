### Mui markup

```html
<Grid container >
  <Grid item container flex-direction='column'>
    columns[0].map(()=>{<Grid item></Grid>}
  </Grid>
  <Grid item container>
    columns[1].map(()=>{<Grid item></Grid>}
  </Grid>
  <Grid item container>
    columns[2].map(()=>{<Grid item></Grid>}
  </Grid>
</Grid>
```